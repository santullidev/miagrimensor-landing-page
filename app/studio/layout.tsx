/**
 * Layout del Studio de Sanity.
 * IMPORTANTE: El Studio debe renderizarse completamente en el cliente.
 * Este layout existe para asegurarse de que no haya conflicto con el layout raíz.
 */
export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
